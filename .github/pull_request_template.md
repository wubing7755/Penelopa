## Summary

- What changed:
- Why:

## Validation

- [ ] `dotnet build Penelopa.sln` — all projects
- [ ] `dotnet test Penelopa.sln` — all xUnit tests pass
- [ ] `dotnet format Penelopa.sln --verify-no-changes` — no style violations
- [ ] Manual smoke: `dotnet run --project src/Penelopa` and inspect the workspace
- [ ] Not run; reason:

## Impact

- Public API impact (`Penelopa.Core` / `Penelopa.Rendering` / `Penelopa`):
- NuGet packaging or dependency impact:
- Documentation impact (`docs/`): keep English canonical and mark the Chinese
  translation `Outdated` when a normative document changes without a
  synchronized translation.

## AI Assistance

- [ ] AI assistance was used and disclosed — docs read, validation run, human verification recorded.
- [ ] No meaningful AI assistance was used.
